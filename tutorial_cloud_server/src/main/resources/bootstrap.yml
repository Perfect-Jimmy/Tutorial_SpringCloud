#server:
  #port: @server.port@
spring:
  application:
    name: register-center
  cloud:
    config:
      uri: http://localhost:8020
      profile: dev
      label: master
      name: register-center

